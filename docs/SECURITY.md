# Security

JWT authentication, bcrypt password hashes, ownership checks, non-enumerable UUID IDs, server-generated storage names, declared-MIME, extension, size, PDF-signature and image-decode validation, plus sanitized API errors are implemented. Set a strong `JWT_SECRET`; production should additionally add malware scanning, rate limits, TLS, and object-storage encryption.
