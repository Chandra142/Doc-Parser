# API workflow

Register, login, supply `Authorization: Bearer <token>`, upload `/api/v1/documents`, poll `/status`, then query questions/review items. Link an answer-key document through the relationships endpoint.
